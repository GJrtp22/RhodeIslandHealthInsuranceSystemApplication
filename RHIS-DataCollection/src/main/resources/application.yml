server:
  port: 3335
spring:
  application:
    name: DC-API
  boot:
    admin:
      client:
        url: http://localhost:8081/
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
management:
  endpoints:
    web:
      exposure:
        include: '*'
        
